"use strict";
{
    let FirstName = "Redwan Hasan";
    let digit = 34;
    let isAdmin = false;
    let x = null;
    let courses = [
        "Web Development",
        "Data Science",
        "Machine Learning",
    ];
    let eligileRoll = [34, 78, 65, 80];
    let d; // this is called the anytype data structure its not good practice for declare anytype.
    d = 8;
    d = "Hello";
    d = [1, 2, 3, 4]; // thats why we are not using the anytype data structure because we can assign different value in one variable.
    let coordinates = [34, 67];
    let ageName = [45, "Redwan"]; // this is called tuple
    console.log("First Name:", FirstName);
    console.log("Digit:", digit);
    console.log("Is Admin:", isAdmin);
    console.log("Courses:", courses);
    console.log("Eligible Roll Numbers:", eligileRoll);
    console.log("x:", x);
    console.log("Coordinates:", coordinates);
    console.log("Age and Name:", ageName);
}
